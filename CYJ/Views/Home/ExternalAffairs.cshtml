
@{
    ViewBag.Title = "External Affairs";
    Layout = "~/Views/Shared/_mytemplate.cshtml";
}

<head>
    <style>
        .graph {
            margin-left: 300px;
        }
    </style>
    <!--Load the AJAX API-->
    <script type="text/javascript" src="https://www.google.com/jsapi"></script>
    <script type="text/javascript">

        // Load the Visualization API and the piechart package.
        google.load('visualization', '1.0', { 'packages': ['corechart'] });

        // Set a callback to run when the Google Visualization API is loaded.
        google.setOnLoadCallback(drawChart);

        // Callback that creates and populates a data table,
        // instantiates the pie chart, passes in the data and
        // draws it.
        function drawChart() {
            // Create the data table. 2
            var data2 = new google.visualization.DataTable();
            data2.addColumn('string', 'Topping');
            data2.addColumn('number', 'Slices');
            data2.addRows([
                ['Elected Officials', 3],
                ['Community Engagements', 1],
                ['Event Attendees', 15],
                ['Volunteers Engaged', 1],
            ]);
            // Set chart options 2
            var options2 = {
                'title': 'EXTERNAL AFFAIRS',
                'width': 400,
                'height': 300
            }
            var chart2 = new google.visualization.BarChart(document.getElementById('chart_dix2'));
            chart2.draw(data2, options6);
        }

    </script>
</head>

<body>
    <!--Divs that will hold the charts-->
    <center><h2>External Affairs</h2></center>
    <br />
    <div>
    <table class="columns" style="margin-left:12%">
        <tr>
            <td><div id="chart_div2" style="border: 1px solid #ccc"></div></td>
        </tr>
    </table>
        </div>
</body>

