@model CYJ.Models.CATEGORY

@{
    ViewBag.Title = "Deactivate";
    Layout = "~/Views/Shared/_mytemplate.cshtml";
}

<h2>Deactivating Category</h2>

<h3>Are you sure you want to deactivate this?</h3>

    <div>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.categoryName)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.categoryName)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> 
                <div class="col-md-3">
                    <input type="button" class="btn btn-info" value="Back to List" onclick="@("window.location.href='" + @Url.Action("Index", "Categories") + "'");" />
                </div>
            </div>
        }
    </div>
