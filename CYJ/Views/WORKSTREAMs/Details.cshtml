@model CYJ.Models.WORKSTREAM

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_mytemplate.cshtml";
}
<h2>View Saved Workstream</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.workstreamName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.workstreamName)
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.workstreamID }) 
    <div class="col-md-3">
        <input type="button" class="btn btn-info" value="Back to List" onclick="@("window.location.href='" + @Url.Action("Index", "Workstreams") + "'");" />
    </div>
</p>
